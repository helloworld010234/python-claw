spring:
  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5432/tinyclaw}
    username: ${DB_USER:tinyclaw}
    password: ${DB_PASSWORD:tinyclaw}
    driver-class-name: org.postgresql.Driver
  flyway:
    enabled: true
    locations: classpath:db/migration

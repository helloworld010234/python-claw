spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/tinyclaw
    username: tinyclaw
    password: ${DB_PASSWORD:tinyclaw}
    driver-class-name: org.postgresql.Driver
  flyway:
    enabled: true
    locations: classpath:db/migration
